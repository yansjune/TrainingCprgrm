#include <stdio.h>

int main() {
    int hour, minute;

    printf("Enter time (HH:MM): ");
    scanf("%d:%d", &hour, &minute);

    if (hour >= 0 && hour <= 23 && minute >= 0 && minute <= 59)
        printf("Valid Time\n");
    else
        printf("Invalid Time\n");

    return 0;
}
